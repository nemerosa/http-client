description = "Wrapper on top of the Apache HTTP Client to provide high level methods."

dependencies {
    compile libraries.http_client
    compile libraries.slf4j_api
    compile libraries.lombok
}
